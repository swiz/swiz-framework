# Versioning
swiz.version = 1.0.0-alpha
swiz.name = swiz-framework-${swiz.version}
swiz.namespace = http://swiz.swizframework.org/

# Setup paths for build
build.libs.loc = ${basedir}/build/libs
src.loc = ${basedir}/src
test.src.loc = ${basedir}/test
libs.loc = ${basedir}/libs
bin.loc = ${basedir}/bin
test.bin.loc = ${basedir}/bin-test
report.loc = ${basedir}/report
report.html.loc = ${basedir}/report/html
docs.loc = ${basedir}/docs
dist.loc = ${basedir}/dist